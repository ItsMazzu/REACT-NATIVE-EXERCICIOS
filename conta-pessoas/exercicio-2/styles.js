import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container:{
    flex: 1,
  },
  titulo:{
    textAlign: 'center',
    fontSize: 20
  },
  contador:{
    textAlign: 'center',
    fontSize: 100,
  }
})

export {styles}